package production;

import java.util.Random;

public class ProductionA implements IProduction {

	@Override
	public int produce() {
		Random random = new Random();
        int randomNumber = random.nextInt(11); // 0 ile 1000 arasında bir rastgele sayı üretiyor
		return randomNumber;
	}

	
}
